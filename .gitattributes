# Set default line ending normalization for text files
* text=auto

# Enforce LF for shell scripts (crucial for gradlew on Linux/macOS)
*.sh text eol=lf
gradlew text eol=lf

# Ensure properties files use LF (common for configuration files)
*.properties text eol=lf

# Treat image files as binary (important to prevent corruption)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.bmp binary
*.ico binary

# Optionally, you might want to specify other common file types:
# *.java text eol=lf
# *.xml text eol=lf
# *.json text eol=lf
# *.yaml text eol=lf